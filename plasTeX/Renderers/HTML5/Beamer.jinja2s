name: frame frameenv
<section class="frame">
{% if obj.title %}<h2 class="frametitle">{{ obj.title }}</h2>{% endif %}
{% if obj.subtitle %}<h3 class="framesubtitle">{{ obj.subtitle }}</h3>{% endif %}
{{ obj }}
</section>

name: frametitle framesubtitle
{# via frame #}

name: note
{# omit #}

name: only visible
<span class="fragment" data-fragment-index="{{ obj.overlay }}">{{ obj }}</span>
